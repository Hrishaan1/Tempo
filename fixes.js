// Small progressive enhancements kept separate from the compact application bundle.
(() => { const pad=n=>String(n).padStart(2,'0'); const key=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`; const parse=k=>{const [y,m,d]=k.split('-').map(Number); return new Date(y,m-1,d,12)}; document.querySelector('#previousDay').addEventListener('click', event => { event.stopImmediatePropagation(); const stored=JSON.parse(localStorage.getItem('tempo.schedule.v2')||'{}'); const day=parse(stored.selectedDate||key(new Date())); stored.selectedDate=key(new Date(day.getTime()-86400000)); localStorage.setItem('tempo.schedule.v2',JSON.stringify(stored)); location.reload(); }, true); })();

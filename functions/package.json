{
  "name": "tempo-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Sends Tempo reminder pushes to signed-in devices via Cloud Messaging.",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0"
  }
}